#!/bin/sh -e

# Connect hook runs BEFORE the connection is established,
# so we CANNOT access confdb here (snapctl get/set will fail or hang).
#
# Initialization of default confdb values is handled in the
# change-view hook, which runs AFTER the connection is established.

logger -t ${SNAP_NAME} "Confdb control interface connected. Initialization will happen on first access."
